#!/usr/bin/python3
"""function returning true or false if the obj is instance"""


def is_same_class(obj, a_class):
    """return true if obj is instance of spcific class"""
    return (type(obj) == a_class)
